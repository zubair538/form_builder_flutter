class User {
  String first_name, email, accept_terms, language, rate, choice, filter,signature;

  User(this.first_name, this.email, this.accept_terms, this.language, this.rate,
      this.choice, this.filter,this.signature);
}
